import React from 'react';

const ShoppingListItem = () => {
    return (
        <div>
            <span>Item</span>
            <button>Remove</button>
        </div>
    );
}

export default ShoppingListItem;